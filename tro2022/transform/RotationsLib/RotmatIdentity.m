% RotmatIdentity.m - Philipp Allgeuer - 05/11/14
% Returns the identity rotation matrix.
%
% function [RI] = RotmatIdentity()
%
% RI ==> Identity rotation in the rotation matrix representation

% Main function
function [RI] = RotmatIdentity()

	% Return the required identity rotation
	RI = eye(3);

end
% EOF